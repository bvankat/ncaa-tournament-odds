/**
 * Site-wide configuration.
 * Toggle IS_OFFSEASON to switch between in-season and offseason mode.
 *
 * Offseason mode:
 *  - Updates pill dot turns red and text is prefixed with "Offseason — "
 *  - Speedometer needle stops bouncing
 */
export const IS_OFFSEASON = true;
